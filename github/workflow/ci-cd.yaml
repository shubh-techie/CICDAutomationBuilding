name: Continuous integration and deployment 

on:
  push:
      branches:
        - main

jobs:
   build:
     runs-on: ubuntu-latest
     steps:
       - name: check out code 
       - uses: actions/checkout@v2